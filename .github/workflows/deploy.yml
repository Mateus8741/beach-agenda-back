# name: Deploy to Coolify

# on:
#   push:
#     branches: [ develop ]
#   workflow_dispatch:

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'
#           cache: 'npm'

#       - name: Install dependencies
#         run: npm install

#       - name: Build
#         run: npm run build

#       - name: Deploy to Coolify
#         run: |
#           curl -X POST \
#             -H "Authorization: Bearer ${{ secrets.COOLIFY_TOKEN_GITHUB }}" \
#             https://coolify.codewaveds.com.br/webhooks/source/github/events/manual 

name: Deploy to Coolify

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Check out code
        uses: actions/checkout@v2
        
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Build the application
        run: npm run build

      - name: Deploy to Coolify
        env:
          COOLIFY_API_TOKEN: ${{ secrets.COOLIFY_TOKEN_GITHUB }}
        run: |
          curl -X POST "https://coolify.codewaveds.com.br/api/deploy" \
          -H "Authorization: Bearer $COOLIFY_API_TOKEN" \
          -H "Content-Type: application/json" \
          -d '{ "project": "i00k8swwgcwgogs4w80sg088" }'